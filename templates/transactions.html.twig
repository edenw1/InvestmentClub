<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}Transaction History{% endblock %}</title>
    <link rel="stylesheet" href="homePageStyles.css">
    <style>
        /* Add styling for stock links */
        .stock-link {
            color: #007bff;
            text-decoration: none;
            font-weight: bold;
        }
        .stock-link:hover {
            text-decoration: underline;
            color: #0056b3;
        }
    </style>
</head>
<body>

    <div class="sidebar">
        <img src="Inv_Logo.png" alt="Investment Club Logo">
        <a href="controller.php" class="button">Back To Home</a>
    </div>

    <div class="content">
        <header>
            <h1>{% block header %}Transaction History{% endblock %}</h1>
        </header>

        <div class="container">
            {% block content %}
            <table class="transactions-table">
                <tr>
                    <th>Stock</th>
                    <th>Type</th>
                    <th>Quantity</th>
                    <th>Price per Share</th>
                    <th>Date</th>
                </tr>
                {% for transaction in transactions %}
                <tr>
                    <td>
                        <a href="/controller.php?action=stock&symbol={{ transaction.stock_symbol }}" class="stock-link">
                            {{ transaction.stock_name }}
                        </a>
                    </td>
                    <td>{{ transaction.transaction_type }}</td>
                    <td>{{ transaction.quantity }}</td>
                    <td>${{ transaction.price_per_share | number_format(2) }}</td>
                    <td>{{ transaction.buy_sell_date }}</td>
                </tr>
                {% endfor %}
            </table>
            {% endblock %}
        </div>

        <footer>
            <p>{% block footer %}&copy; 2025 Stock Tracker. All rights reserved.{% endblock %}</p>
        </footer>
    </div>

</body>
</html>